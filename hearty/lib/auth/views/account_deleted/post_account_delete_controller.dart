/// Defines a contract handling UI events for a post-account-delete page.
abstract class PostAccountDeleteController {
  /// Constant constructor.
  const PostAccountDeleteController();

  /// Called when the user taps the Done button.
  void onDoneTap();
}
