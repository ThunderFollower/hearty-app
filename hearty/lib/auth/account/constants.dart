/// The key used to store the flag indicating if it's the user's first time authenticating.
const firstTimeAuthKey = 'is_first_time_authentication';

/// The key used to store the flag indicating if the current app mode is doctor mode.
const doctorModeKey = 'is_doctor_mode_enabled';
