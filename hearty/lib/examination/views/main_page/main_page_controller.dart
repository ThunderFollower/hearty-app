/// Encapsulates presentation logic for the main page.
abstract class MainPageController {
  /// Handle opening and closing of the [Scaffold.drawer].
  void handleDrawerChange({required bool opened});
}
